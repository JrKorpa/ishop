project.name = diamond
server.index = 192.168.0.23:8383
server.search = 192.168.0.23:8384
project.default_charset = UTF-8

[goods_id]
type = id

[goods_sn]
index = self
tokenizer = full

[goods_name]
type = title
index = both

[goods_number]
index = self
tokenizer = full

[from_ad]
index = self
tokenizer = full

[good_type]
index = self
tokenizer = full

[market_price]
type = numeric
index = self
tokenizer = full

[shop_price]
type = numeric
index = self
tokenizer = full

[member_price]
type = numeric
index = self
tokenizer = full

[chengben_jia]
type = numeric
index = self
tokenizer = full

[carat]
type = numeric
index = self
tokenizer = full

[clarity]
index = self
tokenizer = full

[cut]
index = self
tokenizer = full


[color]
index = self
tokenizer = full

[shape]
index = self
tokenizer = full

[depth_lv]

[table_lv]

[symmetry]
index = self
tokenizer = full

[polish]
index = self
tokenizer = full

[fluorescence]
index = self
tokenizer = full

[warehouse]
index = self
tokenizer = full

[guojibaojia]

[cts]

[us_price_source]

[source_discount]

[cert]
index = self
tokenizer = full

[cert_id]
index = self
tokenizer = full

[gemx_zhengshu]
index = self
tokenizer = full

[status]
index = self
tokenizer = full

[add_time]
index = self
tokenizer = full

[is_active]
index = self
tokenizer = full

[kuan_sn]
index = self
tokenizer = full

[mo_sn]
index = self
tokenizer = full

[pifajia]
type = numeric
index = self
tokenizer = full

[pifajia_mode]
index = self
tokenizer = full

[img]
index = self
tokenizer = full

[is_hot]
index = self
tokenizer = full

[xilie]
index = self

